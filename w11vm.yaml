apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: windows-vdi
spec:
  running: true
  template:
    metadata:
      labels:
        kubevirt.io/domain: windows-vdi
    spec:
      domain:
        cpu:
          cores: 2
        resources:
          requests:
            memory: 4Gi
        devices:
          disks:
            - name: harddisk
              disk:
                bus: virtio
            - name: cdrom-disk
              cdrom:
                bus: sata
            - name: virtio-disk
              cdrom:
                bus: sata
          interfaces:
            - name: default
              masquerade: {}
      networks:
        - name: default
          pod: {}
      volumes:
        - name: harddisk
          persistentVolumeClaim:
            claimName: win-vdi-disk
        - name: cdrom-disk
          persistentVolumeClaim:
            claimName: windows-iso
        - name: virtio-disk
          persistentVolumeClaim:
            claimName: virtio-iso
